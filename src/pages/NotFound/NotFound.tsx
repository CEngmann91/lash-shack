import { PageWrapper } from '../../components'

const NotFound = () => {
    return (
        <PageWrapper title="NotFound">
            NotFound
        </PageWrapper>
    )
}

export default NotFound